package com.ziprun.order;

/**
 * Sprint-2 placeholder: heavy orders will route only to capable agents.
 * Defined now so the {@code weight_class} column and enum exist as a seam.
 */
public enum WeightClass {
    LIGHT,
    HEAVY
}
